// tailwind.config.js is NO LONGER USED with Tailwind CSS v4.
//
// In Tailwind v4, all configuration has moved to CSS.
// Previously this file contained:
//
//   theme.extend.fontFamily.sans    = ["var(--font-dm-sans)", "sans-serif"]
//   theme.extend.fontFamily.display = ["var(--font-syne)",    "sans-serif"]
//
// These are now defined in src/app/globals.css inside the @theme block:
//
//   @theme {
//     --font-sans:    var(--font-dm-sans), ui-sans-serif, system-ui, sans-serif;
//     --font-display: var(--font-syne), ui-sans-serif, system-ui, sans-serif;
//   }
//
// Content paths (for class detection) are automatically inferred by Tailwind v4
// from the project structure — no manual content array needed.
//
// This file is kept as a placeholder/reference only and is NOT read by Tailwind.
